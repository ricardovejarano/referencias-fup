import { Routes } from '@angular/router';
import { RestorePassComponent } from './restore-pass.component';

export const restoreRoute: Routes = [
    {path: 'restablecer', component: RestorePassComponent}
];
