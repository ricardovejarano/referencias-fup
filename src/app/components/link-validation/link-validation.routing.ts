import { Routes } from '@angular/router';
import { LinkValidationComponent } from './link-validation.component';

export const LinkRoutes: Routes = [
    {
        path: 'validation', component: LinkValidationComponent
    }
];
